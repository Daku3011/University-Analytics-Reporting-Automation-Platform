# ============================================================
# SU Analytics - University Analytics Reporting Platform
# Python Dependencies
# ============================================================

# ── Web Framework ────────────────────────────────────────────
Django==5.2.1
gunicorn==23.0.0          # Production WSGI server for HF Spaces
whitenoise==6.9.0         # Serve static files without a separate web server

# ── PDF Generation ───────────────────────────────────────────
# WeasyPrint renders HTML/CSS templates into PDF reports
weasyprint==68.1

# ── AI / Generative AI (gemini-api-dev skill) ────────────────
# NEW: google-genai (replaces deprecated google-generativeai)
# Ref: https://ai.google.dev/gemini-api/docs/migrate.md.txt
google-genai
python-dotenv==1.2.2
markdown==3.10.2

# ── Image Processing ─────────────────────────────────────────
# Pillow is required by Django's ImageField (college logos,
# newspaper clippings, top-post screenshots)
Pillow==12.2.0
python-docx==1.1.2        # DOCX text extraction for Gemini document upload feature
pypdf==5.3.0              # Used to split large PDFs before sending to Gemini API

# ── Background Tasks ─────────────────────────────────────────
celery==5.6.3
redis==7.4.0

# ── Database Drivers ─────────────────────────────────────────
psycopg2-binary==2.9.12   # PostgreSQL driver for Django
dj_database_url           # Parse DATABASE_URL from .env for Django settings